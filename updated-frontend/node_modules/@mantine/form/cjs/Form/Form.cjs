'use client';
'use strict';

var React = require('react');

const Form = React.forwardRef(
  ({ form, onSubmit, onReset, ...others }, ref) => /* @__PURE__ */ React.createElement(
    "form",
    {
      ...others,
      onSubmit: form.onSubmit(typeof onSubmit === "function" ? onSubmit : () => {
      }),
      onReset: (event) => {
        onReset?.(event);
        form.onReset(event);
      },
      ref
    }
  )
);

exports.Form = Form;
//# sourceMappingURL=Form.cjs.map
