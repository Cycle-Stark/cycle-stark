'use client';
import React from 'react';
import { Box, Group, Button } from '@mantine/core';
import { useModals } from './use-modals/use-modals.mjs';

function ConfirmModal({
  id,
  cancelProps,
  confirmProps,
  labels = { cancel: "", confirm: "" },
  closeOnConfirm = true,
  closeOnCancel = true,
  groupProps,
  onCancel,
  onConfirm,
  children
}) {
  const { cancel: cancelLabel, confirm: confirmLabel } = labels;
  const ctx = useModals();
  const handleCancel = (event) => {
    typeof cancelProps?.onClick === "function" && cancelProps?.onClick(event);
    typeof onCancel === "function" && onCancel();
    closeOnCancel && ctx.closeModal(id);
  };
  const handleConfirm = (event) => {
    typeof confirmProps?.onClick === "function" && confirmProps?.onClick(event);
    typeof onConfirm === "function" && onConfirm();
    closeOnConfirm && ctx.closeModal(id);
  };
  return /* @__PURE__ */ React.createElement(React.Fragment, null, children && /* @__PURE__ */ React.createElement(Box, { mb: "md" }, children), /* @__PURE__ */ React.createElement(Group, { justify: "flex-end", ...groupProps }, /* @__PURE__ */ React.createElement(Button, { variant: "default", ...cancelProps, onClick: handleCancel }, cancelProps?.children || cancelLabel), /* @__PURE__ */ React.createElement(Button, { ...confirmProps, onClick: handleConfirm }, confirmProps?.children || confirmLabel)));
}

export { ConfirmModal };
//# sourceMappingURL=ConfirmModal.mjs.map
