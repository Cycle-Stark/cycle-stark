'use client';
'use strict';

var React = require('react');
var core = require('@mantine/core');
var useModals = require('./use-modals/use-modals.cjs');

function ConfirmModal({
  id,
  cancelProps,
  confirmProps,
  labels = { cancel: "", confirm: "" },
  closeOnConfirm = true,
  closeOnCancel = true,
  groupProps,
  onCancel,
  onConfirm,
  children
}) {
  const { cancel: cancelLabel, confirm: confirmLabel } = labels;
  const ctx = useModals.useModals();
  const handleCancel = (event) => {
    typeof cancelProps?.onClick === "function" && cancelProps?.onClick(event);
    typeof onCancel === "function" && onCancel();
    closeOnCancel && ctx.closeModal(id);
  };
  const handleConfirm = (event) => {
    typeof confirmProps?.onClick === "function" && confirmProps?.onClick(event);
    typeof onConfirm === "function" && onConfirm();
    closeOnConfirm && ctx.closeModal(id);
  };
  return /* @__PURE__ */ React.createElement(React.Fragment, null, children && /* @__PURE__ */ React.createElement(core.Box, { mb: "md" }, children), /* @__PURE__ */ React.createElement(core.Group, { justify: "flex-end", ...groupProps }, /* @__PURE__ */ React.createElement(core.Button, { variant: "default", ...cancelProps, onClick: handleCancel }, cancelProps?.children || cancelLabel), /* @__PURE__ */ React.createElement(core.Button, { ...confirmProps, onClick: handleConfirm }, confirmProps?.children || confirmLabel)));
}

exports.ConfirmModal = ConfirmModal;
//# sourceMappingURL=ConfirmModal.cjs.map
