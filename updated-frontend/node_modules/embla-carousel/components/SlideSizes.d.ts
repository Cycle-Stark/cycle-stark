import { AxisType } from './Axis';
export type SlideSizesType = {
    slideSizes: number[];
    slideSizesWithGaps: number[];
};
export declare function SlideSizes(axis: AxisType, containerRect: DOMRect, slideRects: DOMRect[], slides: HTMLElement[], readEdgeGap: boolean): SlideSizesType;
