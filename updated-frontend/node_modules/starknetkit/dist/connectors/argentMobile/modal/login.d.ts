import type { SignClientTypes } from "@walletconnect/types";
import type { NamespaceAdapter, NamespaceAdapterOptions } from "./adapter";
export interface IArgentLoginOptions {
    projectId?: string;
    name?: string;
    description?: string;
    url?: string;
    chainId?: string | number;
    rpcUrl?: string;
    bridgeUrl?: string;
    mobileUrl?: string;
    modalType?: "overlay" | "window";
    walletConnect?: SignClientTypes.Options;
}
export declare const login: <TAdapter extends NamespaceAdapter>({ projectId, chainId, name, description, rpcUrl, bridgeUrl, mobileUrl, modalType, url, walletConnect, }: IArgentLoginOptions, Adapter: new (options: NamespaceAdapterOptions) => TAdapter) => Promise<TAdapter>;
